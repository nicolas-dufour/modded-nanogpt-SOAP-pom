# @package _global_

defaults:
  - override /model: pomgpt


training:
  batch_size: 48
  accumulation: 2
  num_iterations: 1500

model:
  gpt:
    normalize_layers: false

experiment_name: pomngpt_no_norm_layers